[phases.setup]
nixPkgs = ["nodejs_22"]

[variables]
    NPM_CONFIG_UPDATE_NOTIFIER = "false"
    NPM_CONFIG_FUND = "false"

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
